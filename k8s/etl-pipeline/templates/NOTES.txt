You have successfully installed the etl-pipeline for source {{ .Values.source }}, running on the following schedule: {{ .Values.schedule }}.
{{- if .Values.suspend }}
NOTE: The pipeline is currently SUSPENDED and will not run on schedule.
{{- end }}

To uninstall the pipeline, run:
  helm uninstall {{ .Release.Name }}

To suspend the pipeline, run:
  helm upgrade {{ .Release.Name }} . --reuse-values --set suspend=true

To resume the pipeline, run:
  helm upgrade {{ .Release.Name }} . --reuse-values --set suspend=false
