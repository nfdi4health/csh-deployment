ingress:
  dns:
dataverse:
  mem_max_metaspace_size:
  s3:
    access_key:
    secret_key:
    bucket_name:
    endpoint_url:
  oidc:
    issuer:
    cliedid:
    clientsecret:
  resources:
    limits:
      memory: "2Gi"
    requests:
      memory: "500Mi"
images:
  backend: gdcc/dataverse:alpha
  configbaker: gdcc/configbaker:alpha
